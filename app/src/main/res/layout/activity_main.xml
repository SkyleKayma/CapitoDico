<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ca_background"
    android:orientation="vertical">

    <fragment
        android:id="@+id/nav_host_fragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        app:defaultNavHost="true"
        app:navGraph="@navigation/nav_graph" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigationMain"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/shape_bottomnnavbar_background_rounded"
        android:elevation="4dp"
        app:itemIconTint="@color/selector_bottom_navigation_main"
        app:itemTextAppearanceActive="@style/CapitoTheme.TextAppearance.Body2.Medium"
        app:itemTextAppearanceInactive="@style/CapitoTheme.TextAppearance.Body2.Regular"
        app:itemTextColor="@color/selector_bottom_navigation_main"
        app:menu="@menu/bottom_navigation_main" />

</LinearLayout>